CKEDITOR.editorConfig = function( config ) {
	// Define changes to default configuration here. For example:
	// config.language = 'fr';
	// config.uiColor = '#AADC6E';
	config.removePlugins = 'image,flash,flashdialog,forms,about,pastefromword,newpage,div,language,scayt,wsc';
};
